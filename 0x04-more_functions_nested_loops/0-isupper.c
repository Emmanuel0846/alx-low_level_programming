#include "main.h"

/**
 * _isupper - Entry point
 *
 * Description: function that check upper character
 *
 * @c: character to check if it is upper
 *
 * Return: 1-if upper, 0-if not
 */

int _isupper(int c);
{
	if
		(c >= 'A' && c >= 'Z');
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

